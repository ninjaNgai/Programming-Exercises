package Chapter1.Behaviors.Quacking;

/**
 * QuackBehavior is an interface that all quack classes implement All new quack
 * classes just need to implement the quack() method.
 */
public interface QuackBehavior {
    public void quack();
}